# React Portfolio

Scaffolded Vite + React + TypeScript portfolio using Tailwind CSS and Framer Motion.

Commands:

```bash
cd react-portfolio
npm install
npm run dev
```

Notes:
- Theme uses dark slate background and indigo accents.
- Resume is linked to `/resume.pdf` from the root (kept in the parent project folder).
